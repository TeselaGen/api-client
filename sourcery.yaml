# https://docs.sourcery.ai/Configuration/
ignore:
  - "*/dist-packages/*" # Note that any strings beginning with * must be quoted

refactor:
  skip: []

metrics:
  quality_threshold: 25.0

clone_detection:
  min_lines: 3
  min_duplicates: 2
  identical_clones_only: false
